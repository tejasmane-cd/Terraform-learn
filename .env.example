# Optional local notes only — Terraform does not load this file.
# When you configure GitHub Actions, use the checklist (same names, no secrets in git):
#   .github/github-actions.env.example
#
# Add real values in GitHub: Settings → Secrets and variables → Actions.
